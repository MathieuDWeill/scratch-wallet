# Scripts

- `deploy.js`: Hardhat deployment to HashKey Chain.
- `check_hashkey_rpc.py`: verifies RPC connectivity from Python.
- `anchor_sample_decision.py`: sends a sample anchor transaction when env vars are configured.

Use burner wallets only.
